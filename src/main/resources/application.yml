spring:
  datasource:
    url:jdbc :
      mysql: //localhost:3306/gestion2stock
    username: root
    password : root
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
  ## Specifiy DBMS
  jpa:
    database: MYSQL
    show-sql: false
# HIbernate DDL Auto (create, create_drop, update)
  hibernate:
    ddl-auto: update
  properties:
    hibernate:
      dialect : org.hibernate.dialect.MySQL5Dialect
  server:
    port: 8081
